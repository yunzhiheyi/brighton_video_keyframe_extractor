# 插件检查说明

这次我顺手把插件做了一轮基础整理，重点是把命名和工程元数据收口，避免后续接入时出现“包名改了但平台侧还是旧名字”的问题。

本次已处理：

- Flutter 包名统一改为 `brighton_video_keyframe_extractor`
- Android 包名统一改为 `com.brighton.brighton_video_keyframe_extractor`
- 示例工程包名同步改为 `brighton_video_keyframe_extractor_example`
- README、示例 README、测试引用、方法通道引用统一改名
- iOS podspec 准备同步到新命名，并与插件版本保持一致

我额外看到的可继续优化项：

- `podspec` 里的主页、作者信息还是模板值，后面最好补成真实仓库地址和维护者信息
- 如果准备对外发布，建议补一个更完整的 `CHANGELOG` 发布记录
- 若后续要兼容旧接入方，可以再补一层迁移说明，明确旧包名升级到新包名需要改哪些 import
